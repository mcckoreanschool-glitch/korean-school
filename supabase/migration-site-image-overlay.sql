-- ============================================================
--  MCC 한글학교 — 배경 사진 투명도(막 진하기) 조절 마이그레이션
--  사용법: Supabase 대시보드 → SQL Editor → 붙여넣고 RUN (한 번만)
--
--  site_images.overlay: 배경 위 막의 진하기(%)
--  숫자가 낮을수록 사진이 선명하게 보임. 비어 있으면 기본값 85.
--  admin → 사이트 이미지의 슬라이더로 조절.
-- ============================================================

alter table public.site_images
  add column if not exists overlay int;
